module main

import std

int main()
{
    bool is_valid = true
    std.printf("is_valid: %d\n", is_valid)
    
    bool is_false = false
    std.printf("is_false: %d\n", is_false)
    
    return 0
}
